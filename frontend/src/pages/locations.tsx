import { LocationList } from "@/widgets/locations/location-list";

export const LocationsPage = () => {
  return (
    <div className="flex justify-center items-center bg-background w-full p-2">
      <LocationList />
    </div>
  );
};
